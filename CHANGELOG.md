# 변경 이력

버전은 [semver](https://semver.org/lang/ko/)를 따른다.

| 자리 | 언제 올리나 | 관리자가 할 일 |
|---|---|---|
| `1.0.x` | 버그 수정만 | 파일만 덮어쓰면 된다 |
| `1.x.0` | 기능 추가 | 파일만 덮어쓰면 된다 |
| `x.0.0` | 시트 구조 등 되돌리기 어려운 변경 | 릴리스 노트의 안내를 먼저 읽어야 한다 |

## 1.1.0

- 담당 수업 신청에 이름 입력칸을 추가. 관리자의 `교사 담당 신청` 표에 이름과
  이메일이 함께 표시된다.
- 관리자 포털 `교사 관리` 탭에 `담당 신청 알림` 카드를 추가 — 새 담당 신청이
  들어올 때 관리자에게 가는 이메일 알림을 켜고 끌 수 있다(기본값 켜짐).
  꺼도 신청 접수 자체에는 영향이 없다.
- 검사: 체크포인트 2.5 `91/91` · 2.6 `29/29` · 5 `85/85` · 실행형 회귀 `64/64`

## 1.0.1

- 모바일 화면에서 상단 탭 4개가 가로 스크롤 없이 한 줄에 들어오게 조정.
- 헤더에 지금 버전 번호(`v1.0.1`)를 상시 표시.
- 헤더가 스크롤해도 화면에 계속 붙어 있던 것을 없애고, 원래대로 페이지와
  함께 스크롤되게 되돌림.
- 모바일 학생 목록이 한 줄에 2명 대신 3명씩 보이게 카드 폭·여백을 줄임.
- 제목에서 학교명과 "학생 활동 기록"을 줄바꿈으로 분리.
- 관리자 포털 헤더에 "학교 관리는 PC 환경을 추천합니다." 안내를 상시 표시.
- 모바일 우선 앱 취지에 맞춰, 관리자도 모드를 지정하지 않고 접속하면
  관리자 포털 대신 개인 이용자 화면으로 먼저 들어가게 기본값을 바꿈.
  관리자 포털은 "관리자 모드로 돌아가기" 버튼으로 명시적으로 열어야
  하며, 그 탭에서는 새로고침해도 관리자 포털이 유지된다.
- README에 "개인정보 관련 유의사항" 절 추가 — 개인정보 접근 자체는 기존
  동의로 다뤄지는 일이고 이 앱이 새로 만드는 지점은 저장 위치(교사 개인
  계정)라는 것, 저장소(코드)에는 실제 개인정보가 없다는 것, 나이스를
  대체하는 공식 기록 시스템이 아니라는 것을 명시.

## 1.0.0

첫 공개 릴리스.

### 구조

- 학생 명단·증명사진은 **관리자 소유 중앙 자료**에 한 벌만 둔다.
- 활동 기록은 **교사 각자의 Drive에 각자 소유로** 저장한다. 관리자도 남의 기록을
  볼 수 없다.
- 담당 수업을 관리자가 승인하면 그 계정에만 중앙 자료 보기 권한이 자동으로
  부여되고, 해지하면 제거된다.

### 수강 그룹 (고교학점제)

- 학급 단위로 수업하지 않는 과목은 `수강 그룹`으로 신청하고, 승인 후 교사가
  학생을 직접 편성한다.
- 공동교육과정으로 들어온 타교생을 교사가 직접 추가·삭제할 수 있다.
- 그룹 담당의 반은 `0`으로 기록해 학급 단위 담당과 겹치지 않게 한다.
- 수강 그룹은 여러 학년이 함께 들을 수 있다(`함께 듣는 다른 학년`). 관리자가
  삭제·비활성화한 담당은 교사의 신청 이력 화면에도 그 상태가 반영된다.

### 새 학년도

- 학년도는 3월 1일에 저절로 넘어간다. 관리자 화면의 `새 학년도 시작하기`는
  준비 상태를 보여 주고 지난 학년도 자료를 골라 비활성화한다.
- 담당이 끊긴 교사에게 학년도가 바뀌었다는 안내가 뜬다.

### 운영

- 관리자 화면에서 담당 신청을 허용할 도메인·계정을 지정할 수 있다.
- 새 버전이 나오면 관리자 화면에 알린다.

### 검사

체크포인트 2.5 `91/91` · 2.6 `29/29` · 5 `81/81` · 실행형 회귀 `59/59`

---

## 1.0.0 이전

`v8.1`부터 `v8.4`까지는 **공개된 적 없는 내부 개발 세대**다. 앞자리 `8`의 출처는
기록에 남아 있지 않고, 그 번호로 배포된 적도 없어 1.0.0 으로 다시 시작했다.

| 세대 | 내용 |
|---|---|
| v8.4 | 중앙 자료와 교사 소유 개인 기록 분리, 수강 그룹, 담당키 |
| v8.3 | 활성 스프레드시트 의존 제거 |
| v8.2 | 학급 사진을 묶음 하나로 묶어 Drive 호출 축소 |
| v8.1 | 사진 폴더 접근 진단 함수 |

저장소의 `verify-v8.4-*.js` 파일 이름은 이 시절에 붙은 것이다. 파일이 서로를
이름으로 불러 실행하므로 그대로 두었다. 앱 버전과는 관계가 없다.
