# 설치 안내

학교 한 곳에 이 웹앱을 설치하는 순서입니다. 사람이 직접 해야 하는 단계와 도구가
대신할 수 있는 단계를 나눠 적었습니다.

AI 코딩 도구(Claude Code, Codex 등)에게 이 저장소 주소만 주고 설치를 맡길 수
있습니다. 다만 아래 **사람만 할 수 있는 일** 네 가지는 도구가 대신하지 못하므로,
설치하는 관리자가 직접 눌러야 합니다.

## 무엇을 설치하나

교사가 학생 활동을 기록하는 Google Apps Script 웹앱입니다. 저장 구조는 두 갈래로
나뉩니다.

- **중앙 자료** — 학생 명단과 증명사진. 관리자 Drive에 한 벌만 둡니다.
- **개인 기록** — 활동·관찰 기록. 교사 각자의 Drive에 각자 소유로 저장됩니다.

관리자도 다른 교사의 기록 파일에는 접근하지 않습니다. 자세한 구조는
[README.md](README.md)를 보십시오.

## 준비물

- Google 계정 하나 (관리자용). 학교 Google Workspace 계정이면 더 좋고, 개인
  Gmail도 됩니다.
- [Node.js](https://nodejs.org) (clasp를 쓸 경우에만)

## 사람만 할 수 있는 일

도구에게 맡기더라도 아래 네 가지는 관리자가 브라우저에서 직접 눌러야 합니다.

| # | 할 일 | 어디서 |
|---|---|---|
| 1 | Apps Script API 켜기 | [script.google.com/home/usersettings](https://script.google.com/home/usersettings) |
| 2 | `clasp login` 승인 | 브라우저에 뜨는 Google 로그인 창 |
| 3 | `registerInitialSchoolAdministrator` 실행 | Apps Script 편집기의 실행 버튼 |
| 4 | 권한 동의 화면 승인 | 3번을 누르면 뜨는 창 |

4번에서 **"이 앱은 확인되지 않았습니다"** 경고가 나올 수 있습니다. 관리자
본인이 방금 만든 스크립트라서 그런 것이므로, **고급 → (프로젝트 이름)(으)로
이동**을 눌러 계속 진행하면 됩니다.

3번은 일부러 사람만 할 수 있게 만들었습니다. **스크립트 프로젝트에 편집 권한이
있는 계정만** 최초 관리자로 등록됩니다. 웹앱 주소만 아는 사람은 이 함수를 호출해도
관리자가 될 수 없습니다.

## A. clasp으로 설치하기 (권장)

명령 몇 줄로 끝나고, 나중에 코드를 갱신할 때도 편합니다.

```bash
npm install -g @google/clasp
```

```bash
clasp login
```

브라우저가 열리면 관리자 계정으로 승인합니다. 그전에 위 표의 1번(Apps Script API
켜기)을 먼저 해두어야 합니다.

이 저장소를 받은 폴더에서 새 Apps Script 프로젝트를 만듭니다.

```bash
clasp create-script --type webapp --title "학생 활동 기록"
```

이 저장소에는 `.clasp.json`이 없습니다. 위 명령이 그 파일을 새로 만들며, 그
안에 **설치하는 학교 자신의** 스크립트 ID가 들어갑니다.

코드를 올립니다. `.claspignore`가 문서와 검사기를 알아서 제외합니다.

```bash
clasp push
```

여기서 **위 표의 3번·4번을 하십시오.** Apps Script 편집기를 열고
(`clasp open-script`) 함수 목록에서 `registerInitialSchoolAdministrator`를 골라
실행합니다.

배포합니다.

```bash
clasp create-deployment -d "학생 활동 기록 최초 배포"
```

배포 주소를 확인합니다.

```bash
clasp list-deployments
```

`AKfyc...`로 시작하는 배포 ID가 나오면, 교사에게 줄 주소는 다음 형식입니다.

```text
https://script.google.com/macros/s/<배포ID>/exec
```

## B. 손으로 복사해 설치하기

clasp을 쓰지 않으려면 [script.google.com](https://script.google.com)에서 새
프로젝트를 만들고 파일을 하나씩 붙여넣습니다. 어떤 파일을 어떤 형식으로 만드는지는
[DEPLOYMENT_CHECKLIST.md](DEPLOYMENT_CHECKLIST.md) 1장에 표로 정리되어 있습니다.

`appsscript.json`은 **프로젝트 설정 → 매니페스트 파일을 편집기에 표시**를 켜야
보입니다.

## 설치한 다음

여기서부터는 웹앱 화면 안에서 진행됩니다.

1. `/exec` 주소를 **관리자 계정으로** 엽니다. 학교 설치 화면이 나옵니다.
2. 학교명을 입력하고 **새 중앙 자료 만들기**를 고릅니다. 관리자 Drive에 중앙
   스프레드시트와 사진 폴더가 만들어지고, 필요한 시트가 자동으로 생성됩니다.
   - 이미 쓰던 자료가 있으면 **기존 중앙 자료 다시 연결하기**를 고릅니다.
3. 관리자 포털에서 학생 명단을 CSV로 넣습니다.
4. 증명사진을 중앙 사진 폴더에 넣고 **사진 동기화**를 누릅니다. 파일 이름은
   `학년1자리+반1자리+번호2자리` 형식입니다(1학년 1반 1번 → `1101.jpg`).
5. 교사에게 `/exec` 주소만 알려줍니다. **Apps Script 프로젝트는 공유하지
   않습니다.**
6. 교사가 담당 수업을 신청하면 관리자에게 메일이 옵니다. 관리자 포털에서
   승인하면 앱이 그 교사에게 중앙 자료 보기 권한을 자동으로 부여합니다.

## 설치 후 확인할 보안 설정

**중앙 자료는 비공개로 두십시오.** 교사에게 손으로 공유하지 않아도 됩니다. 담당
승인 시 앱이 개별 보기 권한을 자동으로 부여하고, 해지하면 자동으로 거둡니다.
"링크가 있는 모든 사용자"나 그룹·도메인 공유를 걸어두면 앱에서 해지해도 접근이
남습니다.

**누가 담당 신청을 할 수 있는지 정하십시오.** 기본값은 웹앱 주소를 아는 로그인
계정 누구나입니다. 승인 전에는 학생 자료를 볼 수 없지만, 신청 자체는 관리자에게
메일을 보냅니다.

- Google Workspace 학교: 배포 화면에서 **도메인 전용 접근**을 고르는 것이 가장
  확실합니다.
- 개인 Gmail로 설치한 학교: 도메인 옵션을 쓸 수 없으므로 Script Property
  `ALLOWED_TEACHER_EMAILS`에 교직원 이메일이나 `@학교도메인`을 넣습니다.

**승인된 교사는 앱 밖에서도 중앙 자료를 열 수 있습니다.** 앱 화면은 자기 담당
학급만 보여주지만, Drive에서 중앙 스프레드시트를 직접 열면 전교생 명단이 보입니다.
개별 보기 권한을 주는 방식이라 생기는 구조적 한계이며, 학교에 배포하기 전에 이
점을 알고 계셔야 합니다.

## 코드 검사

Google 계정 없이 코드만 확인해 볼 수 있습니다.

```bash
node verify-v8.4.js
```

## 문제가 생기면

배포 관리 화면에서 직전 버전을 다시 고르면 즉시 되돌아갑니다. clasp을 쓴다면
다음과 같습니다.

```bash
clasp redeploy <배포ID> -V <되돌릴 버전 번호>
```

오류를 기록할 때는 시각, 배포 버전, 실행한 함수, 화면에 뜬 오류 코드와 메시지,
재현 단계만 남기십시오. **이메일 주소 전체, 파일 ID, 폴더 ID, 토큰은 기록하지
않습니다.**
