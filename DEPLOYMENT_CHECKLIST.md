# v8.4 체크포인트 5 Apps Script 배포 체크리스트

이 문서는 담당 반 일괄 등록, 교사 자기 담당 신청·관리자 승인, 승인 시 중앙 자료
자동 공유·해지를 포함한 수동 설치 안내서입니다. 실제 Google 배포나 계정 A·B
실환경 검증은 수행하지 않았습니다. `appsscript.json`의 `script.send_mail`
범위는 승인·거부·해지 알림 메일 발송에 필요하다.

## 1. 최종 런타임 파일 복사

Apps Script 프로젝트에 다음 파일을 같은 버전으로 복사합니다. Apps Script 파일
목록에서 `.gs`는 **스크립트**, `Index.html`은 **HTML**로 만듭니다.

| 파일 | 분류 | 역할 |
|---|---|---|
| `Code.gs` | 필수 | 웹앱 `doGet`, 공통 기록 기능, 개인 저장소 진입 |
| `CentralData.gs` | 필수 | 현재 사용자 확인, 중앙 학생·사진·담당 조회, Script Properties |
| `InstallationSetup.gs` | 필수 | 최초 관리자 등록과 학교 설치 상태 판정 |
| `AdminPortal.gs` | 관리자 전용 필수 | 중앙 시트 점검·생성, 학생·담당·사진 관리자 기능 |
| `AssignmentRequests.gs` | 필수 | 교사 자기 담당 신청, 관리자 승인·거부, 처리 이력 |
| `PersonalRouting.gs` | 필수 | 현재 사용자 개인 데이터베이스·폴더 라우팅 |
| `PersonalStorage.gs` | 필수 | 개인 데이터베이스 생성·검증·기록 저장소 |
| `SchoolSetup.gs` | 필수 | 개인 데이터베이스 초기 시트 생성, CSV 파싱·정제 |
| `CourseGroups.gs` | 필수 | 수강 그룹 학생 편성, 공동교육과정 타교생 추가 |
| `Index.html` | 필수 | 웹앱 화면과 `google.script.run` 호출 |
| `appsscript.json` | 필수(프로젝트 설정) | V8, 웹앱 실행 사용자, 접근 범위, OAuth scope |

`appsscript.json`은 Apps Script 편집기에서 **프로젝트 설정 → 매니페스트 파일
"appsscript.json"을 편집기에 표시**를 켠 뒤 내용을 교체합니다. 파일명은
`Index.html`의 대문자 `I`까지 정확히 일치해야 합니다.

## 2. 복사하지 않는 파일

| 파일 | 분류 | 이유 |
|---|---|---|
| `README.md` | 문서 전용 | Apps Script 런타임 파일이 아님 |
| `INSTALL.md` | 문서 전용 | Apps Script 런타임 파일이 아님 |
| `DEPLOYMENT_CHECKLIST.md` | 문서 전용 | Apps Script 런타임 파일이 아님 |
| `LICENSE` | 문서 전용 | Apps Script 런타임 파일이 아님 |
| `verify-v8.4*.js` (검사기 5개) | 테스트 전용 | 로컬 회귀 검사기이며 웹앱 실행 파일이 아님 |
| `.claspignore` `.gitignore` | 도구 설정 | 저장소·clasp 설정이며 웹앱 실행 파일이 아님 |

`clasp push`를 쓰면 `.claspignore`가 위 파일들을 자동으로 제외하므로 따로
신경 쓰지 않아도 됩니다. 손으로 복사할 때만 위 표를 확인하십시오.

## 3. Script Properties 설정

새 프로젝트는 Apps Script 편집기에서 `registerInitialSchoolAdministrator`를 한 번
실행합니다. 웹앱 배포 전·후 모두 실행할 수 있지만 프로젝트 편집 권한이 있는
계정만 최초 관리자로 저장됩니다. 이미 `ADMIN_EMAILS`가 있는 기존 프로젝트는 이
단계를 건너뜁니다. 이후 웹앱 설치 화면에서 학교명을 입력하고 새 자료 생성 또는
기존 자료 재연결을 선택합니다. 기존 자료 재연결은 관리자 계정으로 열 수 있는
스프레드시트·폴더의 URL 또는 ID를 사용합니다. 실제 ID와 이메일을 문서나 로그에
기록하지 않습니다.

| 키 | 값 형식 | 목적 |
|---|---|---|
| `CENTRAL_DATABASE_FILE_ID` | 중앙 스프레드시트 **파일 ID만** | 중앙 학생·사진정보·교사담당·앱설정 조회 |
| `CENTRAL_PHOTO_FOLDER_ID` | 중앙 사진 폴더 **폴더 ID만** | 사진 파일 검증 및 동기화 |
| `ADMIN_EMAILS` | 이메일 1개 이상 | 관리자 역할 판정 |
| `STAFF_GROUP_EMAIL` | 선택: 교직원 Google 그룹 이메일 | 운영 메모용. 인증·자동 공유·관리자 판정에는 사용하지 않음 |
| `ALLOWED_TEACHER_EMAILS` | 선택: 이메일 또는 `@도메인` 목록 | 담당 신청을 할 수 있는 계정 제한. 비우면 로그인 계정 누구나 신청 가능 |
| `CENTRAL_SCHEMA_VERSION` | `3` | 수강그룹 시트를 포함한 중앙 스키마 버전 |

빈 설정표:

```text
CENTRAL_DATABASE_FILE_ID = <중앙 스프레드시트 파일 ID>
CENTRAL_PHOTO_FOLDER_ID = <중앙 사진 폴더 ID>
ADMIN_EMAILS = <관리자 이메일>
STAFF_GROUP_EMAIL = <선택: 교직원 Google 그룹 이메일>
CENTRAL_SCHEMA_VERSION = 3
```

`ADMIN_EMAILS`는 코드의 `split(/[\s,;]+/)` 규칙을 따릅니다. 즉 쉼표, 공백,
줄바꿈, 세미콜론을 구분자로 사용할 수 있습니다.

```text
ADMIN_EMAILS = admin1@example.com, admin2@example.com
```

이메일은 서버에서 trim 후 소문자로 정규화하고 정확히 일치시킵니다. 파일 ID는
Drive URL 전체가 아니라 ID 부분만 입력합니다.

`ALLOWED_TEACHER_EMAILS`를 비워 두면 웹앱 URL을 아는 로그인 계정은 누구나 담당
신청을 할 수 있습니다. 승인 전에는 학생·사진에 접근하지 못하지만 관리자에게
알림 메일이 가고 신청 이력이 남으므로, 학교 정책상 제한이 필요하면 교직원
이메일이나 `@학교도메인`을 넣습니다. Workspace 학교는 배포 화면의 도메인 전용
접근이 더 확실하며, 개인 Gmail로 설치한 학교는 이 속성을 사용합니다.

Google 그룹 없이 웹앱 링크를 배포하는 경우 `STAFF_GROUP_EMAIL`은 비워 둡니다.
웹앱 링크를 아는 것만으로 중앙 Drive 파일의 권한이 생기지는 않으며, 관리자가 담당
신청을 승인할 때 앱이 해당 Gmail 계정에 개별 보기 권한을 자동 부여합니다.

## 4. 중앙 자료 공유

중앙 자료는 비공개 상태로 유지하고 다음을 확인합니다.

1. 관리자 개인 계정이 중앙 스프레드시트와 사진 폴더를 열 수 있는지 확인
2. 승인 전 테스트 교사 계정은 두 자료에 접근할 수 없는지 확인
3. 담당 승인 후 앱이 테스트 교사 계정에 두 자료의 **보기** 권한을 추가했는지 확인
4. 담당 해지 후 앱이 해당 계정의 직접 보기 권한을 제거했는지 확인
5. 제거 실패가 표시되면 비활성 교사 행의 `Drive 권한 제거 재시도`를 실행하고,
   중앙 DB와 사진 폴더의 실패 상세가 각각 해소되는지 확인
6. 공유 대상에서 제외된 계정은 중앙 자료에 접근할 수 없는지 확인

다음 설정은 사용하지 않습니다.

- 링크가 있는 모든 사용자에게 공개
- 웹에 공개
- 교사 계정에 편집 권한 부여
- 교직원 그룹·도메인 단위 공유
- 사진 폴더의 파일별 공개 링크 배포

앱의 담당 수업 필터는 Drive ACL을 대체하지 않습니다. 실제 파일 접근 범위는
Drive 공유 설정이 결정합니다.

## 5. 웹앱 배포 설정

`appsscript.json` 기준값은 다음과 같습니다.

```text
실행 사용자: 웹 앱에 액세스하는 사용자
기본 접근 범위: ANYONE (Google 계정 로그인 사용자)
런타임: V8
```

배포 화면에서는 학교 운영 정책에 맞게 로그인 사용자를 제한합니다. 학교 도메인
제한이 필요한 경우 배포 대화상자의 도메인 전용 옵션을 선택하고,
익명 접근(`ANYONE_ANONYMOUS`)은 선택하지 않습니다.

배포 방식은 다음 중 하나입니다.

- 기존 `/exec` URL 유지: **배포 → 배포 관리 → 기존 웹앱 편집 → 새 버전 → 배포**
- 새 URL 발급: **배포 → 새 배포 → 웹앱**. 기존 URL은 이전 배포로 남음

실제 배포는 아직 수행하지 않았습니다. 배포 후 `/exec` URL이 로그인 계정에서
열리는지 확인합니다.

## 6. 수동 설치 순서

1. 위의 런타임 파일을 복사합니다.
2. `Code.gs`, `Index.html`, manifest 파일명을 확인합니다.
3. 새 프로젝트라면 Apps Script 편집기에서
   `registerInitialSchoolAdministrator`를 한 번 실행합니다.
4. 기존 배포의 새 버전 또는 새 웹앱을 배포합니다.
5. `/exec` URL을 관리자 계정으로 열어 학교명을 입력하고 다음 중 하나를 선택합니다.
   - `새 중앙 자료 만들기`: 관리자 Drive에 중앙 DB와 사진 폴더 생성
   - `기존 중앙 자료 다시 연결하기`: 기존 스프레드시트와 사진 폴더 URL 또는 ID 입력
6. 중앙 자료를 비공개 상태로 유지하고 관리자 계정의 접근을 확인합니다.
7. 중앙 `학생목록`, `사진정보`, `교사담당`, `담당신청`, `수강그룹`, `수강그룹원`,
    `앱설정` 시트가 생성되었는지 확인합니다. 기존 설치는 관리자 포털의
    `중앙 시트 점검·생성`을 한 번 눌러야 신설 시트가 만들어집니다.
8. 관리자 화면에서 여러 반을 일괄 등록한 뒤 담당 수가 즉시 갱신되는지 확인합니다.
9. 교사 계정 B에서 자기 담당을 신청하고 관리자 A가 승인한 뒤, B에게 중앙 자료
   보기 권한과 승인된 담당만 표시되는지 확인합니다.
10. 관리자 A가 B의 권한을 해지한 뒤 담당 비활성화와 중앙 자료 직접 보기 권한
    제거를 확인합니다. 제거 실패 시 화면에 표시된 자원별 실패 원인을 기록하고,
    비활성 교사 행에서 권한 제거를 재시도합니다.

11. 오류가 발생하면 시각, 배포 버전, 실행 함수, 화면 오류 코드·메시지, 브라우저와
    오류를 재현한 단계만 기록합니다. 전체 이메일, 파일 ID, 폴더 ID, 토큰, 비밀번호는
    기록하지 않습니다.
12. 문제가 있으면 배포 관리에서 직전 정상 버전을 다시 선택해 배포합니다. Git
    변경을 되돌릴 때는 다음과 같이 커밋을 되돌립니다.

```text
git revert <이번 기능 커밋 해시>
```

## 7. 비파괴 설치 초기화

관리자 포털의 `설치 설정 초기화`는 중앙 DB ID, 구형 호환 DB ID, 사진 폴더 ID,
중앙 스키마 버전, 학교명과 설치 완료 상태만 재설정합니다.

- `ADMIN_EMAILS` 유지
- 기존 중앙 스프레드시트·사진 폴더 유지
- 교사 개인 DB·폴더 유지
- 담당 신청 Script Properties 삭제
- 초기화 후 현재 관리자가 설치 화면으로 이동

Drive 파일을 삭제하거나 휴지통으로 보내지 않습니다. 기존 파일을 실제로 폐기할
때는 소유자가 Drive에서 직접 확인 후 처리합니다. 초기화 전 사용하던 중앙 자료를
계속 쓰려면 `기존 중앙 자료 다시 연결하기`에서 해당 링크를 입력합니다.

## 8. 설치 전 로컬 확인

저장소 루트에서 다음 검사를 실행합니다. Node.js만 있으면 되고 Google 계정은
필요 없습니다.

```powershell
node .\verify-v8.4.js
node .\verify-v8.4-cp5.js
node .\verify-v8.4-runtime.js
```

`verify-v8.4.js`는 `.gs` 구문, 중복 함수, 매니페스트, 공개 함수 계약, 개인 기록
보존과 학생·사진의 중앙 이동을 검사합니다. 통과하면 확인 문구 한 줄만 출력하고
종료 코드 0으로 끝납니다.

`verify-v8.4-cp5.js`는 `RESULT 62/62`를 출력하며, 안에서 `-cp2.6.js`(`29/29`)와
`-cp2.5.js`(`91/91`)를 다시 실행하므로 이 하나로 세 검사가 모두 수행됩니다.

`verify-v8.4-runtime.js`(`13/13`)는 `.gs`를 실제로 불러와 함수를 호출합니다. 위
검사기들은 소스에 특정 문자열이 있는지만 보므로, 코드는 다 있는데 동작이 죽는
결함은 이 검사기만 잡습니다. 로컬 통과는 Google 실환경 권한 검증을 의미하지
않습니다.

## 9. 활동기록 열 추가(담당키) 안내

`활동기록`·`활동기록보관` 시트 끝에 `담당키` 열이 추가되었습니다. 교사가 웹앱에
접속하면 자동으로 추가되므로 손으로 할 일은 없습니다.

배포 직후 이미 열려 있던 화면에서는 머리글 확인 오류가 한 번 날 수 있습니다.
**새로고침하면 해결됩니다.** 배포 안내에 이 한 줄을 함께 알려 주십시오.

이 열이 비어 있는 기존 기록은 예전처럼 학년·반·과목으로 담당을 판정하므로
조회·수정·삭제가 그대로 됩니다.

제작·배포·관리: 부산 동아공고 류기현
